================================================================================
CORTEX — REMAINING LAUNCH WORK (two tracks: FOUNDER + HERMES AGENT)
================================================================================
Written 2026-07-04, after: domain purchased (https://trydoppl.com), a GitHub
organization exists for the website + app, and all launch CODE/DOCS are built.
This file is the single source of truth for "what's left." It has two tracks:

  TRACK F — FOUNDER: things only a human with accounts/money/identity can do.
  TRACK H — HERMES AGENT: things an AI coding agent (repo + shell access) can do.

Each item has: WHY, EXACT STEPS, DEPENDS ON, DONE-WHEN. Items are numbered so
you can say "Hermes, do H3 and H7" or "I finished F4, unblock H2."

Throughout, the assumed names are:
  Site:            https://trydoppl.com
  API:             https://api.trydoppl.com
  Support email:   support@trydoppl.com   Privacy: privacy@trydoppl.com
Replace if you choose different ones (tell Hermes so it updates the repo).

--------------------------------------------------------------------------------
STATUS SNAPSHOT — what is ALREADY DONE (do not redo)
--------------------------------------------------------------------------------
Backend + product (all tested, ~1069 backend tests green, pushed to the
sarptandoven/cortex-by-doppl remote, branch mass-scale-app-redesign):
  - Local-first macOS app (builds, signs ad-hoc) + hosted multi-tenant backend.
  - Accounts: email+password (argon2id), GitHub/Google OIDC, sessions with
    rotation/revocation, password reset, account delete, self-serve token mint.
  - Per-user encryption (envelope, crypto-erase on delete); connector creds
    encrypted; enforce flag (CORTEX_REQUIRE_ENCRYPTED_CREDENTIALS).
  - Web account front door at /account/* (signup/login/verify/reset/home).
  - SMTP email sender (beta autoverify mode needs no email at all).
  - In-app "Sign in to Cortex Cloud" (email/pw + browser Google/GitHub).
  - Billing webhook (Paddle) + plan-based quotas — dormant until configured.
  - Context assembly engine + curated MCP tool surface for AI tools.
  - deploy/ kit: one-command VM setup (Caddy HTTPS, systemd, nightly backups).
  - Notarization tooling + Developer-ID hardened-runtime entitlements.
  - Launch gate script (scripts/public_launch_gate.py).
  - Legal/support drafts: privacy.html, terms.html, subprocessors.html, faq.html,
    incident + KEK-escrow runbooks (placeholders to fill; lawyer to review).
  - CI workflow exists (.github/workflows/ci.yml) — but has NEVER RUN yet.

The remaining work is: (a) external accounts/actions only you can do, and
(b) a handful of automation/wiring tasks a Hermes agent can do in the repo.

--------------------------------------------------------------------------------
DECISION 0 — READ THIS FIRST (blocks several items)
--------------------------------------------------------------------------------
The code currently lives on the personal remote sarptandoven/cortex-by-doppl.
You now have a GitHub ORG for the product. You must decide the canonical repo,
because CI, GitHub Pages, GitHub Releases (free DMG hosting), and the status
page should all live in the ORG.

  OPTION A (recommended): make the ORG repo (e.g. doppl-tech/cortex or
    doppl-tech/cortex-app) the canonical home. Push the mass-scale-app-redesign
    branch there, open a PR into main, let CI run, merge.
  OPTION B: keep developing on the personal remote and only mirror releases to
    the org. Simpler now, messier later.

ACTION (you): pick A or B and tell Hermes the exact org repo URL. Until then,
Hermes keeps pushing to sarptandoven/cortex-by-doppl (the standing rule) and
will NOT push to the org without your explicit go-ahead.

================================================================================
TRACK F — FOUNDER TASKS (only you can do these)
================================================================================
Ordered by dependency. The ones marked [START NOW] have external waiting periods;
begin them today even if you do nothing else.

F1. [START NOW] LEGAL ENTITY + BUSINESS INBOXES
  WHY: needed to be the named data controller, to sign vendor terms, to take
       money, and to have real support/privacy contacts.
  STEPS:
    a. Form the company (Stripe Atlas ~$500, or a local incorporation).
    b. Create Google Workspace (or similar); make support@trydoppl.com and
       privacy@trydoppl.com. Monitor both.
    c. Give Hermes the exact LEGAL ENTITY NAME and JURISDICTION (e.g. "Delaware,
       USA") so it can fill the placeholders in terms.html / subprocessors.html
       (task H5).
  DEPENDS ON: nothing.  DONE-WHEN: entity exists; both inboxes receive mail.

F2. [START NOW] APPLE DEVELOPER ID CERTIFICATE + NOTARY CREDENTIALS
  WHY: to ship a DMG that opens with a double-click (no scary Gatekeeper prompt).
  STEPS:
    a. Confirm your Apple Developer enrollment. Ideally it is an ORGANIZATION
       enrollment under the company (needs a free D-U-N-S number, ~1–2 wks). A
       personal enrollment works for a beta but plan to move to Org before a big
       push (changing Team ID later resets users' macOS permission grants).
    b. developer.apple.com -> Certificates -> "+" -> "Developer ID Application".
       Create the certificate-signing request from Keychain Access (Certificate
       Assistant -> Request a Certificate from a CA), upload it, download the
       .cer, double-click to install into your login keychain.
    c. Terminal: security find-identity -v -p codesigning
       Copy the full quoted name: "Developer ID Application: NAME (TEAMID)".
    d. appleid.apple.com -> Sign-In & Security -> App-Specific Passwords ->
       generate one labeled "cortex-notary". Copy it.
    e. Terminal: xcrun notarytool store-credentials cortex-notary \
         --apple-id "<your-apple-id>" --team-id "<TEAMID>" --password "<app-pw>"
  DEPENDS ON: nothing (D-U-N-S only if going Org).
  DONE-WHEN: `security find-identity` shows the Developer ID cert; the notary
             profile "cortex-notary" is stored.

F3. [START NOW] EMAIL PROVIDER + SENDING-DOMAIN WARM-UP
  WHY: public launch verification/reset emails must reach inboxes; a cold domain
       gets spam-filtered, so start warming NOW.
  STEPS:
    a. Sign up for Postmark (easiest) or Amazon SES.
    b. Add sending domain mail.trydoppl.com (or trydoppl.com). Add the DNS
       records it shows: SPF (TXT), DKIM (CNAMEs), DMARC (TXT at _dmarc, start
       p=quarantine). Verify them (turns green).
    c. Send yourself + a few friends real mail over several days to build
       reputation before opening public signups.
    d. Note the SMTP host/username/password/from-address for F5 (env config).
  DEPENDS ON: F7 (domain — done) and access to trydoppl.com DNS.
  DONE-WHEN: domain verified in the provider; test mail lands in Gmail+Outlook
             inboxes (not spam).

F4. GITHUB ORG SETUP (unlocks CI, Pages, Releases, status page)
  WHY: your CI, download hosting, site, and status page should live in the org.
  STEPS (after DECISION 0):
    a. Ensure the canonical repo exists in the org.
    b. Repo -> Settings -> Actions -> General: allow Actions to run.
    c. Repo -> Settings -> Branches: add a branch-protection rule on main
       requiring the CI checks (python, security-scan, distribution, macos-app)
       to pass before merge.
    d. Create the repo/org SECRETS Hermes will need for release + status
       automation (Hermes will tell you the exact names in H2/H6; typically a
       token with contents:write for Releases, and any Pages settings).
    e. If using GitHub Pages for the site or status page: Settings -> Pages,
       enable it for the branch/folder Hermes configures.
  DEPENDS ON: DECISION 0.  DONE-WHEN: Actions run on push/PR; branch protection on.

F5. PROVISION THE SERVER + DEPLOY (the backend goes live)
  WHY: this is the hosted backend + accounts, live on the internet.
  STEPS:
    a. Create a Hetzner Cloud account (or DigitalOcean/Lightsail). New project ->
       Add Server: Ubuntu 24.04, type CX32 (~€6.80/mo) for a real beta, your SSH
       key added, "Backups" checkbox ON. Note the public IP.
    b. In trydoppl.com DNS, add:  A record, name "api", value <SERVER-IP>
       (and AAAA with the IPv6 if given). Wait until
       `dig +short api.trydoppl.com` returns the IP.
    c. From the repo on your Mac (or hand SSH access to Hermes — see H11):
         deploy/push.sh root@<SERVER-IP> api.trydoppl.com
       It sets up Caddy HTTPS + systemd + nightly backups and PRINTS your admin
       token + encryption KEK ONCE.
    d. For PUBLIC launch (not beta), before/after deploy set in
       /etc/cortex/cortex.env:  CORTEX_AUTH_AUTOVERIFY=0 and the SMTP_* values
       from F3, then: systemctl restart cortex-api cortex-worker
  DEPENDS ON: F7(domain, done), F3(if public). Notarization (F2) is independent.
  DONE-WHEN: https://api.trydoppl.com/health and /ready are green;
             https://api.trydoppl.com/account/signup loads in a browser.

F6. ESCROW THE ENCRYPTION KEK (do immediately after F5)
  WHY: lose it = all encrypted user data is unrecoverable; leaked-with-a-backup =
       exposure. This is the single most important custody step.
  STEPS: copy the KEK (printed by deploy, also at /etc/cortex/kek) into (1) your
    password manager and (2) a separate offline location NOT co-located with the
    server backups. Follow docs/runbooks/KEK_ESCROW.md.
  DEPENDS ON: F5.  DONE-WHEN: two escrow copies exist; you've read the runbook.

F7. DOMAIN — DONE (trydoppl.com purchased). You only add DNS records as F3/F5
    instruct.

F8. OAUTH LOGIN APPS
  WHY: "Continue with GitHub/Google".
  STEPS:
    a. GitHub (5 min, no review): github.com -> Settings -> Developer settings ->
       OAuth Apps -> New. Homepage https://trydoppl.com ; callback
       https://api.trydoppl.com/v1/auth/oauth/github/callback . Copy client id +
       secret into /etc/cortex/cortex.env (CORTEX_OIDC_GITHUB_CLIENT_ID/SECRET),
       restart cortex-api.
    b. Google (1–2 wk review): Google Cloud -> APIs & Services -> Credentials ->
       OAuth client ID (Web); redirect
       https://api.trydoppl.com/v1/auth/oauth/google/callback . Configure the
       consent screen (add your privacy + terms URLs), submit for verification.
       Set CORTEX_OIDC_GOOGLE_CLIENT_ID/SECRET, restart.
  DEPENDS ON: F5 (server), F1 (privacy/terms URLs live for Google).
  DONE-WHEN: buttons appear on /account/login and a round-trip signs you in.

F9. PUBLISH THE DOWNLOAD SITE
  WHY: somewhere for people to download the app + read privacy/terms.
  OPTIONS (Hermes preps the config in H2/H3):
    a. Cloudflare Pages: connect the repo, output dir "site", map trydoppl.com.
    b. GitHub Pages (org): Settings -> Pages, serve the site/ folder.
  Then put the NOTARIZED DMG where the site links (H2 wires GitHub Releases so
  the binary is NOT in git).
  DEPENDS ON: F2 (notarized DMG), F4 (org), H2/H3.
  DONE-WHEN: trydoppl.com loads; Download works from another computer; checksum
             matches.

F10. BUILD + NOTARIZE THE DMG (one command, after F2)
  STEPS (on your Mac, in the repo):
    cd macos
    CORTEX_CODESIGN_IDENTITY="Developer ID Application: NAME (TEAMID)" \
    CORTEX_NOTARY_PROFILE=cortex-notary CORTEX_BUNDLE_PYTHON=1 \
    ./package_release.sh --production
  If Apple rejects the first submission for an entitlement, the log names it; the
  DeveloperID.entitlements file already covers the common bundled-Python ones, so
  this should pass. Hand the produced DMG + checksums to H2 (Releases) / F9.
  DEPENDS ON: F2.  DONE-WHEN: `spctl -a -t open <dmg>` says accepted.

F11. MONITORING + OFFSITE BACKUP ACCOUNTS
  WHY: know about failures before users do; survive a dead box.
  STEPS:
    a. UptimeRobot (free): monitor https://api.trydoppl.com/ready every 5 min ->
       phone/email alert.
    b. healthchecks.io (free): make a check; give Hermes the ping URL to wire
       into the backup job (set BACKUP_PING_URL).
    c. Offsite backup: create a Hetzner Storage Box or Backblaze B2/Cloudflare R2
       bucket; give Hermes the rclone remote details (H11 wires BACKUP_RCLONE_
       REMOTE) OR do `rclone config` on the box yourself.
    d. Do a RESTORE DRILL (docs/runbooks/KEK_ESCROW.md) before public launch.
  DEPENDS ON: F5.  DONE-WHEN: alert fires on a forced failure; a backup restores.

F12. BILLING ACCOUNT (only if charging at launch)
  STEPS: apply to Paddle (recommended, handles tax) -> create product "Cortex Pro
    $20/mo" -> put the webhook secret into /etc/cortex/cortex.env
    (CORTEX_BILLING_PROVIDER=paddle, CORTEX_PADDLE_WEBHOOK_SECRET=...), set the
    webhook URL to https://api.trydoppl.com/v1/billing/webhook, and ensure your
    checkout passes the Cortex user_id in custom_data.cortex_user_id. Restart.
  DEPENDS ON: F1, F5.  DONE-WHEN: a live checkout flips a test user to "pro".

F13. LEGAL REVIEW + HUMAN TESTS (final gates)
  a. Lawyer reviews privacy.html + terms.html (esp. the two flagged clauses);
     remove the "draft" banners after sign-off.
  b. Usability test: 5–8 non-technical people install + reach a cited answer
     unaided in <10 min. Fix what trips them; re-test.
  c. Manually click through the Mac app's "Sign in to Cortex Cloud" once against
     https://api.trydoppl.com (the one path no automated test covers).
  DEPENDS ON: most of the above.  DONE-WHEN: lawyer signs off; usability passes.

FOUNDER CRITICAL PATH (do in this order, parallelize the [START NOW]s):
  F1, F2, F3 today (they have waiting clocks) -> F4 -> F5 -> F6 -> F8/F10 ->
  F9/F11 -> F12(if paid) -> F13 -> LAUNCH.

================================================================================
TRACK H — HERMES AGENT TASKS (an AI coding agent does these in the repo)
================================================================================
Rules for Hermes:
  - Work in the repo; run the full backend test suite (`python3 -m pytest
    backend/tests -q`) before committing anything backend; build the app with
    `cd macos && ./build.sh` after Swift changes.
  - Do NOT push to the GitHub org until the founder confirms DECISION 0. Keep
    using the existing sarptandoven/cortex-by-doppl remote (or whatever the
    founder names).
  - Every task below is either UNBLOCKED (do now) or BLOCKED-ON: <founder item>.
  - Commit each task separately with a clear message; end commit messages with
    the Co-Authored-By trailer already used in this repo.

H1. [UNBLOCKED] MAKE CI ACTUALLY CORRECT + PROVE IT
  WHY: .github/workflows/ci.yml has never run and uses `python -m unittest
       discover backend/tests`, which bypasses conftest.py's pytest fixtures —
       so CI may be red even though `pytest` is green.
  STEPS:
    a. Change the "Run backend unit tests" step to `python -m pytest backend/tests
       -q` (pytest is the project's real runner). Keep the eval-gate, security,
       distribution, obsidian, macos jobs.
    b. Ensure the workflow installs everything the tests need (it installs
       backend/requirements.txt; confirm argon2-cffi + cryptography are there —
       they are — and add backend/runtime-requirements.txt if any test imports
       need it).
    c. Run the equivalent locally in a clean checkout to confirm green.
    d. (With founder go-ahead on DECISION 0) open a PR into main so CI fires;
       report the run URL. If any job is red on the runner, fix it.
  DEPENDS ON: c is unblocked; d is BLOCKED-ON: F4 (Actions enabled).
  DONE-WHEN: CI is green on a real GitHub Actions run; branch protection can
             require it.

H2. [UNBLOCKED to build; publish BLOCKED-ON F2/F10] GITHUB RELEASES DISTRIBUTION
  WHY: the DMG is currently committed inside git (bloat) and there's no real
       release flow. GitHub Releases = free binary hosting in your org.
  STEPS:
    a. Write scripts/publish_release.sh (or a release.yml workflow triggered on a
       version tag) that: takes the notarized DMG + .app.zip + checksums from the
       package_release output, creates/updates a GitHub Release for the tag via
       the gh CLI, uploads the binaries as release assets, and rewrites
       site/downloads/latest.json so the artifact `url` fields point at the
       release asset URLs (keeping filename/size/sha256).
    b. Document the release procedure in docs/DISTRIBUTION.md.
    c. Tell the founder exactly which repo secret / token scope the workflow
       needs (contents:write) so they can add it (F4d).
  DEPENDS ON: publishing needs F2/F10 (a real notarized DMG) + F4 (org).
  DONE-WHEN: a tag produces a GitHub Release with the DMG attached and
             latest.json points at it.

H3. [UNBLOCKED] TAKE THE BINARIES OUT OF GIT
  WHY: 37 MB of stale, unnotarized DMG/zip are tracked in site/downloads/.
  STEPS:
    a. `git rm --cached site/downloads/*.dmg site/downloads/*.app.zip`; add them
       to .gitignore.
    b. Update scripts/check_distribution_site.py so that when an artifact `url`
       is an absolute https URL (a Release/CDN), the on-disk existence + size +
       hash checks are skipped (or made optional via a flag), while still
       validating latest.json structure. Keep the checksums.txt requirement.
    c. Keep latest.json + checksums.txt in git (small).
    d. Run check_distribution_site.py + the CI distribution job locally -> green.
  DEPENDS ON: pairs with H2 (so downloads still resolve). Safe to do together.
  DONE-WHEN: no binaries in git; the distribution checker still passes.

H4. [UNBLOCKED] DOMAIN-CONSISTENCY PASS
  WHY: make trydoppl.com / api.trydoppl.com the concrete, correct values
       everywhere and remove placeholder/localhost assumptions from user-facing
       copy and operator docs.
  STEPS:
    a. In deploy/README.md, deploy/cortex.env.example, and
       docs/COMPLETE_LAUNCH_INSTRUCTIONS.txt: ensure examples use
       api.trydoppl.com and the correct OAuth callback URLs
       (https://api.trydoppl.com/v1/auth/oauth/{github,google}/callback) and the
       email from-address @trydoppl.com.
    b. Grep the repo for stray "example.com", "YOUR-DOMAIN", localhost-in-
       user-copy; fix any that are meant to be the real domain (leave genuine
       localhost/dev defaults alone).
    c. Ensure the macOS app's default hosted URL suggestion (the Cortex Cloud
       sign-in field) is https://api.trydoppl.com (verify in
       macos/Sources/CortexCloudAuth.swift; build.sh after).
  DEPENDS ON: nothing.  DONE-WHEN: one grep shows no wrong-domain user-facing
             strings; app builds.

H5. [BLOCKED-ON F1] FILL LEGAL PLACEHOLDERS
  WHY: terms.html / subprocessors.html have [LEGAL ENTITY NAME], [JURISDICTION],
       vendor-name and refund/liability placeholders.
  STEPS: when the founder provides the entity name, jurisdiction, chosen email
    provider (Postmark/SES), billing provider (Paddle/Stripe), and whether any
    cloud AI model is enabled, replace every placeholder. Do NOT remove the
    "draft — pending legal review" banners (only the founder's lawyer clears
    those). Re-run scripts/public_launch_gate.py.
  DEPENDS ON: F1 (values).  DONE-WHEN: no [PLACEHOLDER] tokens remain; site
             checker green.

H6. [UNBLOCKED to scaffold; deploy BLOCKED-ON F4] STATUS PAGE
  WHY: a public status.trydoppl.com so users can check outages; runs free on
       GitHub Actions + Pages in the org.
  STEPS:
    a. Scaffold an "upptime" configuration (a small YAML) monitoring
       https://api.trydoppl.com/ready and https://trydoppl.com. Either as a
       dedicated org repo or a workflow in the canonical repo.
    b. Document what the founder enables (Pages + a token secret) in F4.
  DEPENDS ON: deploy needs F4.  DONE-WHEN: status page builds + shows the checks.

H7. [UNBLOCKED] SIGNUP ABUSE HARDENING (Cloudflare Turnstile)
  WHY: public signup will attract bots; argon2 is CPU-heavy so a flood is a DoS
       risk. Add a captcha, dormant until keys are set.
  STEPS: add Turnstile to the /account/signup web page + a server-side siteverify
    step in the signup handler, gated on config (CORTEX_TURNSTILE_SITEKEY /
    CORTEX_TURNSTILE_SECRET). When unset, signup behaves exactly as today. Add
    tests (verify pass/fail; unconfigured = no-op). Keep it stdlib (urllib) for
    the siteverify call.
  DEPENDS ON: nothing to build; founder adds keys later.  DONE-WHEN: with keys
             set, a missing/invalid Turnstile token is rejected; without keys,
             behavior unchanged; tests green.

H8. [UNBLOCKED] STRIPE BILLING PROVIDER (optional second option)
  WHY: billing.py has a Paddle provider + a Stripe seam. Some founders prefer
       Stripe.
  STEPS: implement StripeBillingProvider (webhook signature verification +
    event mapping to plan transitions) behind CORTEX_BILLING_PROVIDER=stripe,
    mirroring the Paddle tests. Dormant unless configured.
  DEPENDS ON: nothing.  DONE-WHEN: stripe tests pass; both providers selectable.

H9. [OPTIONAL / LARGER] SPARKLE AUTO-UPDATE FOR THE MAC APP
  WHY: so users get updates without re-downloading the DMG manually.
  STEPS: integrate Sparkle 2 (EdDSA-signed appcast) reading the latest.json /
    an appcast produced by package_release; wire a "check for updates". This
    needs signing keys the founder generates; scope it and stop for those.
  DEPENDS ON: F2/F10 + founder signing keys.  DONE-WHEN: a built app can detect
             and apply a newer signed build (verified on a real Mac by founder).

H10. [BLOCKED-ON F5 staging] LOAD TEST AGAINST THE REAL BOX
  WHY: the 10k load test ran locally; re-run it against a deployed staging server
       (TLS + argon2 + encryption change the profile).
  STEPS: once a staging server exists, run
    python3 scripts/load_test_10k.py --users 10000 --duration 300 \
      --concurrency 64 --base-url https://staging.trydoppl.com
    Record the result (error rate, p95) in docs/HOSTED_DEPLOYMENT.md.
  DEPENDS ON: F5 (a staging deploy).  DONE-WHEN: pass recorded.

H11. [BLOCKED-ON F5 + credentials] DEPLOY / OPS EXECUTION (only if given access)
  WHY: if the founder grants SSH access to the provisioned box, Hermes can run
       the deploy, wire offsite backups (BACKUP_RCLONE_REMOTE) + the healthchecks
       ping (BACKUP_PING_URL), flip public-launch env (autoverify off + SMTP),
       and run the launch gate against the live server.
  STEPS: only with explicit founder authorization + credentials. Then:
    deploy/push.sh, set env, restart services, run
    `python3 scripts/public_launch_gate.py --base-url https://api.trydoppl.com`.
  DEPENDS ON: F5 + founder-granted SSH.  DONE-WHEN: launch gate automated checks
             green against the live server.

HERMES SUGGESTED ORDER (unblocked first, no founder input needed):
  H1 (CI correctness) -> H4 (domain pass) -> H3+H2 (binaries out of git +
  Releases flow) -> H7 (Turnstile) -> H8 (Stripe) -> H6 (status page scaffold).
  Then, as founder items clear: H5 (legal values), H10 (load test), H11 (deploy),
  H9 (Sparkle, optional).

================================================================================
HOW THE TWO TRACKS MEET (the launch, in order)
================================================================================
1. Founder: F1/F2/F3 started (waiting clocks running).            [day 0]
2. Hermes: H1, H4, H3+H2, H7, H8 done + pushed.                   [day 0–2]
3. Founder: F4 (org/CI on) -> Hermes H1(d) proves CI green + H6 status page.
4. Founder: F5 (deploy) + F6 (KEK escrow) -> API live.
5. Founder: F8 (GitHub login now, Google submitted) + F10 (notarized DMG) ->
   Hermes H2 publishes the Release -> Founder F9 (site live).
6. Founder: F11 (monitoring + restore drill), F12 (billing if paid),
   Hermes H10 (load test on staging).
7. Founder: F5 flip to public (autoverify off + SMTP), Hermes H5 (legal values).
8. Founder: F13 (lawyer sign-off + usability test + manual app sign-in click).
9. Run `python3 scripts/public_launch_gate.py --base-url https://api.trydoppl.com
   --profile public` -> all automated checks green + every MANUAL item confirmed.
10. LAUNCH. Watch UptimeRobot + the status page; keep the incident runbook handy.

Beta shortcut: steps 4 + 5(GitHub login) + a right-click-to-open DMG gets a
private beta live TODAY without waiting on Apple/Google/legal/billing.
================================================================================
END. Keep with the repo as docs/REMAINING_LAUNCH_WORK.txt. See also
docs/COMPLETE_LAUNCH_INSTRUCTIONS.txt (the deep how-to) and the runbooks.
================================================================================
